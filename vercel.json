{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist/voluntariado_ambiental",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}